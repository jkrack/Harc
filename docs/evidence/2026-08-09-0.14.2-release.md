# Harc 0.14.2 Release Evidence

**Date:** 2026-08-09  
**Tag and source:** `v0.14.2` at
`81261a70fe31b85edc8b065f4e126e08f953f57c`  
**GitHub publication:** 2026-08-10T01:53:56Z  
**Notary submission:** `01aa3ae6-33eb-4202-9e0b-a6bf7c553f66`

Harc 0.14.2 build 57 is the published macOS resident-Host release. It repairs
retained client-upload recovery and makes relay Keychain authorization an
explicit user action without blocking local Host startup. This record does not
promote HarcMobile or secondary-Mac Client qualification; those remain separate
decisions in the architecture and physical evidence matrices.

## Exact published artifacts

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `Harc-local.dmg` | 66,150,809 | `92035f41274b23216c0d0b4c3424f6311cb405040963250b40e1d0c41b00a5c2` |
| `Harc-local-dmg.zip` | 63,379,093 | `7ddcabe1c8e3304a1cf1e42a80075e507a51781e160636e7a08ee7a5d932b6c7` |

GitHub's release API reported both assets uploaded on the public, non-draft,
non-prerelease `Harc v0.14.2` release. Its server-side SHA-256 digests and byte
lengths match the retained local release artifacts exactly.

The published appcast enclosure points to the DMG above with build `57`, short
version `0.14.2`, byte length `66150809`, and Sparkle EdDSA signature:

```text
zUptNw2W8aqTFM1lgiwh8ZaOGwloAAQqx4rSHyGQgV0iInOA/TFkeDrVkho1AK/G5F8zUDyGxocSUFFFkuxYCQ==
```

## Validation

- The bounded unrestricted package regression passed all 1,543 Swift Testing
  cases in 259 suites with zero failures or issues.
- The focused canonical-ingest suite passed 14 tests, including the 16
  crash-point parameter cells.
- The HarcAppBridge suite passed all 9 tests.
- The final app-only Keychain authorization expansion compiled successfully in
  the two-worker Xcode Release build.
- `git diff --check`, appcast XML validation, and the final release verifier
  passed before publication.

## Trust and package verification

The final release candidate was Developer ID signed with hardened runtime and a
secure timestamp, accepted by Apple notarization, stapled, and reverified:

- DMG signature and internal checksum valid;
- stapled notarization ticket valid;
- Gatekeeper accepted both the DMG and mounted app as Notarized Developer ID;
- app, `harc-stt`, `harc-mcp`, Sparkle framework, updater, and nested services
  passed strict signature validation;
- bundle `com.harc.Harc`, version/build `0.14.2 (57)`, and arm64 app/helpers
  matched the release contract; and
- the packaged relay origin is `https://relay.adaptcontext.com`.

## Decision boundary

This closes the macOS 0.14.2 artifact and Sparkle publication. It does not close
the broader host/client/mobile goal. Physical two-network relay and revocation
evidence, real secondary-Mac qualification, and HarcMobile's independent
App Store and physical-device matrices remain open.
